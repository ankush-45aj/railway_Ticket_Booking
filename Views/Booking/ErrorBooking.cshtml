@{
    ViewData["Title"] = "Booking Error";
}

<div class="container mt-4">
    <h2>Booking Failed</h2>
    <p class="text-danger">@ViewBag.Error</p>

    <a class="btn btn-primary" href="@Url.Action("Search", "Train")">Back to Train Search</a>
</div>